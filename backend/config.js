const config = {
    db: {
      host: "localhost",
      port: 5432,
      user: "admin",
      password: "password123",
      database: "monkey_rider",
      connectTimeout: 60000
    }
  };

  module.exports = config;